[project]
name = "alpha-actions"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "discord-webhook>=1.4.1",
    "pyyaml>=6.0.3",
    "typer>=0.20.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]

[tool.uv]
exclude-newer = "P30D"
